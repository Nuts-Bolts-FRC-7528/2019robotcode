package frc.robot.components;






public class elevator {
    private static int level = 1;
    private static int goal = 1;


    public void iterate() {
        //update level when halleffevt or clicker thing is wrekt
        if (level > goal) {
            

        }
        else if (level < goal) {

        }
    }
    //mutator method
}